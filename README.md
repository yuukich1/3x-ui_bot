# 3x-UI Telegram Bot

Telegram bot для управления VLESS клиентами на панели 3x-UI.

## Особенности

- ✅ Создание VLESS клиентов
- 📊 Получение информации о клиентах
- 👥 Управление пользователями через Telegram
- 🔐 Безопасное хранение данных

## Установка

```bash
# Установка зависимостей
pip install -r requirements.txt

# Или через Poetry
poetry install
```

## Конфигурация

Создайте файл `.env` в корневой папке:

```env
TELEGRAM_TOKEN=your_token_here
THREEX_HOST=127.0.0.1
THREEX_PORT=8080
THREEX_USERNAME=your_username
THREEX_PASSWORD=your_password
THREEX_HASH_PANEL=your_hash
THREEX_SPX=2F
DATABASE_URL=sqlite+aiosqlite:///db.sql
INBOUND_ID=1
LOG_LEVEL=INFO
```

## Запуск

```bash
python main.py
```

## Команды бота

- `/start` - Начало работы
- `/help` - Справка
- `/create` - Создать VLESS ключ
- `/vless` - Получить VLESS ключ
- `/remove` - Удалить VLESS ключ (в разработке)
- `/online` - Список онлайн клиентов (в разработке)

## Структура проекта

```
3x-ui_bot/
├── config.py              # Конфигурация
├── main.py                # Точка входа
├── database/              # Слой работы с БД
│   ├── base.py            # Конфигурация БД
│   ├── models.py          # ORM модели
│   └── crud.py            # CRUD операции
├── service/               # Бизнес-логика
│   ├── threex_ui_client.py  # API клиент
│   ├── vless_service.py     # Сервис VLESS
│   └── handlers.py        # Обработчики команд
└── schemas/               # Pydantic модели
    ├── clients.py
    ├── inbounds.py
    ├── settings.py
    └── vless.py
```

## Лицензия

MIT
